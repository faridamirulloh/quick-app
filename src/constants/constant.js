export const APP_NAME = 'My Boiler Plate';

export const DEV_MODE = !process.env.NODE_ENV || process.env.NODE_ENV === 'development';

export const HOST = 'reqres.in';
export const URL_HOST = `https://${HOST}`;
